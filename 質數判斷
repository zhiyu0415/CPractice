#pragma warning(disable:4996) 
#include <stdio.h> 
#include<stdlib.h> 
#include<math.h>
#include<stdbool.h>

bool isPrime(int);

int main() {
	int num;
	while (scanf("%d", &num) != EOF) {
		int flag = 0;
		if (isPrime) {
			if (flag < 10) {
				printf("%d", num);
				flag++;
			}
			else {
				flag = 0;
				printf("%d", num);
				flag++;
			}
		}printf("\n");
	}return;
		}
	
	bool isPrime(int num) {
		if (num == 0) {
			return -1;
		}
		else {
			for (int i = 2; i <= num; i++) {
					if (num % i == 0) {
						return 0;
					}
					else {
						return 1;
					}
				
			}
		}
	}
