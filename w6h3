#include <stdio.h>
#include <string.h>

int main() {
	int num[10] = { 0 };
	int max = 0;
	for (int i = 0; i < 10; i++) {
		scanf("%d", &num[i]);
	}
	for (int j = 0; j < 10; j++) {
		if (num[j] > max) {
			max = num[j];
		}
	}
	printf("Largest element is: %d", max);
}
