#include <stdio.h>    
#include<stdlib.h>    
int main() {
	int num = 0;
	int min;
	while (scanf("%d", &min) != EOF) {
		for (int i = 1; i < 10; i++) {
			scanf("%d", &num);
			if (min > num) {
				min = num;
			}
		}printf("Smallest is %d\n", min);
	}
	
}
